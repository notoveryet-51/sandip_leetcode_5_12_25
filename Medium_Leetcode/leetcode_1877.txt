Leetcode 1877

class Solution {
    public int minPairSum(int[] nums) {
        int n=nums.length;
        Arrays.sort(nums);
        int i=0, j=n-1;
        int mx=Integer.MIN_VALUE;
        while (i<j) {
            mx=Math.max(mx,nums[i]+nums[j]);
            i++;    j--;
        }
        return mx;
    }
}