Leetcode 3513

class Solution {
public:
    int uniqueXorTriplets(vector<int>& nums) {
        int res=nums.size();
        if (nums.size()<3) {
            return res;
        }
        int k=31-__builtin_clz(nums.size()); // the most significant bit position
        return 1<<(k+1);
    }
};