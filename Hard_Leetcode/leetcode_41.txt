Leetcode 41

class Solution {
public:
    int firstMissingPositive(vector<int>& nums) {
        int n=nums.size();
        vector<int> hash(n+2, 0);
        for (auto it: nums) if (it>0 && it<=n) hash[it]++;
        for (int i=1; i<=n; i++) if (hash[i]==0) return i;
        return n+1;
    }
};