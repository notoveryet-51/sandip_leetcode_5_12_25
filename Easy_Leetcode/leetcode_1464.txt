Leetcode 1464

class Solution {
public:
    int maxProduct(vector<int>& nums) {
        int a=nums.size();
        sort(nums.begin(),nums.end());
        int product=(nums[a-1]-1)*(nums[a-2]-1);
        return product;        
    }
};