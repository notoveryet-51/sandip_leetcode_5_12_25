Leetcode 1299

class Solution {
public:
    vector<int> replaceElements(vector<int>& arr) {
        int n=arr.size();
        vector<int> nums(n);
        nums[n-1]=-1;
        for (int i=n-2; i>=0; i--) {
            nums[i]=max(nums[i+1], arr[i+1]);
        }
        return nums;
    }
};