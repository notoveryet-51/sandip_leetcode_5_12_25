Leetcode 3996

class Solution {
public:
    bool canReach(vector<int>& start, vector<int>& target) {
        int check1=start[0]+start[1];
        int check2=target[0]+target[1];
        if (check1%2==0 && check2%2==0 || check1%2==1 && check2%2==1) return true;
        else return false;
    }
};